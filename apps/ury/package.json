{
  "private": true,
  "scripts": {
    "pos-install": "cd pos && yarn install --check-files",
    "pos-build": "cd pos && yarn build",
    "postinstall": "yarn pos-install",
    "build": "yarn pos-build"
  },
  "devDependencies": {
    "@types/qz-tray": "^2.2.2"
  }
}
